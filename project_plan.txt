📌 PROJECT CONTEXT SNAPSHOT — ai-slop-gate

Checkpoint: Start of Stage 2.2 (Observation Layer)

🗂 Repository Overview
Branches

main (Python — active)
Основна гілка. Реалізується чиста Python-архітектура.

Stage 1 (core invariants) — зафіксовано

Stage 2 — в процесі

TypeScript повністю винесений в main-ts

main-ts (legacy / reference)
Стара TypeScript-реалізація.

Містить робочий GitHub Actions workflow

Є приклади Stage 2.2 / 2.3 логіки

Використовується лише як референс, не для продакшн-коду

🧱 Current Python Architecture (main)
Core domain (Stage 1 — frozen)

decision.py

class DecisionMode(Enum):
    ADVISORY = "advisory"
    BLOCKING = "blocking"

@dataclass(frozen=True)
class Decision:
    """
    Stage 1 invariant:
    Decision is derived ONLY from policy + observations
    """
    mode: DecisionMode
    reasons: List[str]


Status:
✅ Decision модель стабільна
✅ Provider не має доступу до Decision
❌ Policy evaluator ще не реалізований

🧠 Conceptual Architecture (Locked)
Layer responsibilities (strict)
LLM Provider
   ↓
Observation Layer        ← Stage 2.1 / 2.2 (NOW)
   ↓
Policy Evaluation        ← Stage 2.3
   ↓
Decision (advisory/blocking)
   ↓
Workflow / GitHub Action

🔐 Provider Contract (ABSOLUTELY FIXED)
Providers are SENSORS, not judges

❌ Providers must NOT:

emit PASS / FAIL

emit advisory / blocking

apply policy

short-circuit workflow

✅ Providers MUST emit ONLY structured observations.

Canonical Data Structures
@dataclass(frozen=True)
class Observation:
    category: Literal["quality", "style", "hallucination"]
    signal: Literal["positive", "neutral", "negative"]
    confidence: float        # 0.0 – 1.0
    message: str
    evidence:
        file: str | None
        line: int | None

@dataclass(frozen=True)
class ProviderObservation:
    provider: str            # e.g. "gemini", "local-llama", "mock"
    model: str
    observations: list[Observation]
    raw_text: str            # original LLM output (for audit/debug)


➡️ Only this shape may cross the provider boundary

🚦 Stage Status
✅ Stage 1 — Core invariants

CLI scaffold exists

Decision model frozen

Providers not allowed to decide

🔄 Stage 2.1 — Provider Infrastructure

Gemini provider exists (works)

Uses deprecated google.generativeai (needs upgrade)

Local provider not implemented

Mock provider exists / planned

🟡 Stage 2.2 — Observation Layer (CURRENT STAGE)

Goal:
Providers must emit ProviderObservation + Observation[]

Required changes (NOT DONE YET):

❌ Remove PASS / FAIL / judgement text from Gemini provider

❌ Stop providers from referencing policy

❌ Parse raw LLM text → structured observations

✅ Providers return only signals + metadata

Design rule:

Observation layer is lossy but structured
Raw text is preserved, decisions are forbidden

⏭ Stage 2.3 — Policy Evaluation (NEXT)

Consume observations

Apply YAML policy rules

Produce Decision(advisory | blocking)

Deterministic, testable, provider-agnostic

⚠️ Known Violations (Must Be Fixed in Stage 2.2)

Gemini provider currently:

emits PASS / FAIL

embeds judgement in text

Provider output violates contract

Policy logic partially implicit in provider

🧪 Testing Constraints

Paid APIs mostly unavailable

Need mock provider:

deterministic

no network

emits fixed observations

CLI must support:

--provider mock

--dry-run

📡 GitHub Actions (Reference)
Exists in main-ts

Trigger on PR

Run CLI

Post advisory result

To be ported to Python

Python CLI execution

Advisory mode only

No paid API dependency

🧭 Immediate Next Actions (After Session Restore)

Implement Observation & ProviderObservation models

Refactor Gemini provider:

raw output → structured observations

zero decision logic

Add MockProvider (baseline)

Add provider interface contract tests

Freeze Stage 2.2

🛡️ Non-Negotiable Guardrails

Providers NEVER decide

Policy NEVER sees raw LLM text (only observations)

Workflow NEVER inspects provider internals

Default mode = advisory

Everything must be reproducible offline

🧠 One-Line Summary

Providers observe.
Policy decides.
Workflow enforces.

📍 Checkpoint saved: Before any code changes in Stage 2.2
📌 Use this snapshot to resume work in a new session without context loss.