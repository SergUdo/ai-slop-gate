# ai-slop-gate — Canonical Architecture (from snapshot v7.2)

**!!All architectural decisions are defined in ai_slop_gate_snapshot.json**


This document is an autogenerated human-readable projection of:

👉 `ai_slop_gate_snapshot.json` (version 1.2)

If any part of this document diverges from the snapshot —  
**the snapshot is the single source of truth.**

---

## Project

**Name:** ai-slop-gate  
**Stage:** Stage 7 
**Status:** canonical  
**Purpose:** Policy-driven AI, supply‑chain and compliance gate for code and infrastructure

---

## Architectural Principles

- No hidden logic  
- Policy is the source of truth  
- Compliance is a sidecar  
- Engine is pure  
- CLI is thin  
- Tests lock contracts, not behavior  

---

## Execution Model

### High-level Flow

1. CLI parses flags  
2. Policy is loaded and resolved  
3. Providers collect observations  
4. Compliance sidecar optionally runs  
5. Policy engine evaluates observations  
6. Decision is produced  
7. Reporters render output  
8. Exit code is derived from decision  

---

## CLI

**Entrypoint:** `python -m ai_slop_gate.cli.main`  
**Commands:** `init`, `run`

### Flags

| Flag | Description |
|------|-------------|
| `--policy` | Path to policy.yml (required for run) |
| `--provider` | Provider name (static, k8s-runtime, supply-chain, llm) |
| `--input-file` | Input file for providers |
| `--k8s-manifests` | Kubernetes YAML manifests |
| `--compliance` | Force enable compliance sidecar |
| `--profile` | Override compliance profile |
| `--enforcement` | never \| advisory \| blocking |
| `--verbose` | Emit full resolved state and observations |
| `--github-checks` | Emit GitHub Checks |
| `--github-repo` | GitHub repository |
| `--github-sha` | Commit SHA |
| `--pr-id` | Pull Request ID |

---

## Engine

**Type:** function-based  
**Entry:** `run_analysis(args) -> Decision`

### Responsibilities

- load policy  
- resolve effective compliance config  
- invoke providers  
- merge observations  
- invoke policy engine  
- return decision  

### Restrictions

- no IO formatting  
- no printing  
- no exit calls  

---

## Policy

**Format:** `policy.yml`  
**Versioned:** yes  

### Sections

- enforcement  
- ai_provider  
- compliance  
- profiles  
- rules  

### Rules Engine

- **Inputs:** observations  
- **Outputs:** `Decision(mode, reasons)`  

---

## Compliance Sidecar

**Role:** sidecar  
**Enabled by:**  
- `policy.compliance.enabled`  
- `--compliance` flag  

### Capabilities

- forbidden license detection  
- secret detection  
- data residency enforcement (EU)  

### Profiles

| Profile | Description |
|---------|-------------|
| default | inherits base compliance config |
| eu | enforces GPL/AGPL ban and EU residency |
| eu-strict | blocking enforcement |

---

## Providers

**Contract:** `collect(input) -> list[Observation]`  
**Builtins:** static, k8s-runtime, supply-chain, llm  
**Cache:** optional via `CachedProvider`

---

## Observations

**Immutable:** yes  

### Fields

- category  
- signal  
- confidence  
- evidence  
- source  

---

## Decision Model

### Modes

- allow  
- advisory  
- blocking  

### Exit Codes

| Mode | Exit Code |
|------|-----------|
| allow | 0 |
| advisory | 0 |
| blocking | 1 |

---

## Reporters

- stdout → human-readable  
- github_pr → PR comment  
- github_checks → check-run annotations  

---

## Non-goals

- automatic fixing  
- code rewriting  
- business logic inside CLI  
- provider-specific policy logic  

