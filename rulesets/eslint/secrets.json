{
  "plugins": ["no-secrets"],
  "rules": {
    "no-secrets/no-secrets": [
      "error",
      {
        "tolerance": 4,
        "ignoreContent": [
          "test",
          "example",
          "dummy",
          "localhost"
        ]
      }
    ]
  }
}
